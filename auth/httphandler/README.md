# Auth HTTP Handlers

The brief handler documentation used for OpenAPI descriptions now lives in [AUTH.md](AUTH.md).

`RegisterAuthHandlers` serves the public auth, OIDC, and protected-resource endpoints documented there.
