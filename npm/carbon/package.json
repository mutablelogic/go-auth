{
  "name": "carbon-bundle",
  "private": true,
  "dependencies": {
    "@carbon/web-components": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "build": "esbuild index.js --bundle --format=esm --outfile=dist/bundle.js --minify"
  }
}